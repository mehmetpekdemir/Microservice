server:
  port: ${SERVER_PORT:8888}

spring:
  cloud:
    config:
      server:
        git:
          uri: 
        native:
          search-locations: classpath:/microserviceconfig
          
  profiles:
    active: native